FROM node:20-slim

WORKDIR /app

COPY frontend/package*.json ./
RUN npm install

COPY frontend/ .

EXPOSE 5173

ENV VITE_API_BASE=http://localhost:8000/api/v1

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]